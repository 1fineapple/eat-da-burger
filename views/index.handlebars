<h1>Burgers!</h1>

<h2>Burgers that have not been devoured!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
    {{/unless}}
  {{/each}}
</ul>

<form class="create-form">
  <div class="form-group">
    <label for="ca">Devour it!!!</label>
    <button type="submit" class="burgerButton">Submit</button>
  </div>

</form>

<h2>Burgers that have been devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add Burger!</h2>
<form class="create-form">
  <div class="form-group">
    <label for="ca">Burger:</label>
    <button type="submit" class="burgerButton">Submit</button>
    <input type="text" id="ca" name="name">
  </div>

</form>